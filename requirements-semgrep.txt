# Semgrep is installed in an isolated venv because its dependency pins conflict
# with pip-audit in the shared security scanner environment.
semgrep==1.161.0

# Transitive vulnerability floor for Semgrep's MCP dependency.
python-multipart>=0.0.26
