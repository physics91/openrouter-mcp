# Security scanner versions are pinned because CI parses their JSON reports.
# Update these intentionally with evaluator fixture/test refresh.
safety==3.7.0
bandit==1.9.4
pip-audit==2.10.0

# Transitive vulnerability floors for scanner environments.
h11>=0.16.0
h2>=4.3.0
httpx>=0.28.1
idna>=3.7
pip>=26.1.1
tqdm>=4.66.3
