interface:
  display_name: "mcp-config-ops"
  short_description: "Use when installing or repairing Claude Desktop or Claude Code MCP config, checking status, backing up config files, or fixing config drift in openrouter-mcp"
